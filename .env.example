# Copy this to .env and fill in paths for local development.
# .env is git-ignored and never committed.

VAULT_DIR=/home/professor/notes
DATA_DIR=./data
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Login rate limiting
LOGIN_MAX_FAILURES=5        # consecutive failures before lockout
LOGIN_LOCKOUT_MINUTES=15    # lockout duration in minutes
